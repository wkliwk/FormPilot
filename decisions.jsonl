{"date":"2026-03-27","decision":"Field explanation cache uses DB (PostgreSQL via Prisma) rather than Redis or in-memory","why":"No Redis in the stack; DB cache is persistent across deploys and serverless function restarts (Vercel), which is important for a cost-reduction cache. Acceptable latency since Prisma batch lookups are fast and cache misses still fall through to Claude anyway."}
{"date":"2026-03-27","decision":"Cache key is normalized_label:type (e.g. first_name:text) rather than including form context","why":"Field explanations for common fields like First Name or Email are universal — they don't depend on which form they appear in. Global keying maximises cross-form reuse and is the main cost-saving mechanism."}
{"date":"2026-03-27","decision":"Cache is applied as a post-pass overlay over the Claude response rather than skipping Claude for cached fields","why":"Claude still provides the form title, description, field IDs, and estimatedMinutes which are form-specific. Skipping Claude entirely would require a two-phase design (first extract structure, then enrich). The overlay approach is simpler and already reduces cost once cached data is used — future work can skip Claude for fully-cached forms."}
{"date":"2026-03-27","decision":"CORS utility (src/lib/cors.ts) grants access to chrome-extension://* by origin prefix rather than a static allowlist","why":"Chrome extension IDs are generated per-installation for unpacked extensions and change when re-packed. Checking the scheme prefix (chrome-extension://) instead of exact ID means the deployed API works with any installation of the extension without per-ID config. Production URL is still scoped via NEXT_PUBLIC_APP_URL env var."}
